---
# VORLAGE - NICHT COMMITTEN!
# TEST-Umgebung: n8n DB-Credentials
# Auf k8s-mgmt-10:
# 1. Passwort generieren: openssl rand -base64 24
# 2. Verschluesseln:
#      cd ~/git/eneg-k8s-infrastructure-v2/kubernetes/environments/test/cnpg-secrets
#      sops --encrypt --age age1fdqtcha9jnzqafe5t6hed6v5sv858x2tt6nwuw00u3luyxuaqcxqh5mcrm \
#           --encrypted-regex '^(data|stringData)$' \
#           n8n-db-credentials.yaml > n8n-db-credentials.enc.yaml
#      rm n8n-db-credentials.yaml
apiVersion: v1
kind: Secret
metadata:
  name: n8n-db-credentials
  namespace: databases
  labels:
    app.kubernetes.io/part-of: cloudnative-pg
    app.kubernetes.io/managed-by: argocd
type: Opaque
stringData:
  username: "n8n"
  password: "HIER_GENERIERTES_PASSWORT_EINTRAGEN"
