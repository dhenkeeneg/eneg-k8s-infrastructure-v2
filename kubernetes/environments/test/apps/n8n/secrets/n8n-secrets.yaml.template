---
# VORLAGE - NICHT COMMITTEN!
# TEST-Umgebung: n8n Secrets
# Auf k8s-mgmt-10:
# 1. Encryption Key: openssl rand -hex 32
# 2. DB-Passwort: Dasselbe wie in test/cnpg-secrets/n8n-db-credentials
# 3. Verschluesseln:
#      cd ~/git/eneg-k8s-infrastructure-v2/kubernetes/environments/test/apps/n8n/secrets
#      sops --encrypt --encrypted-regex '^(data|stringData)$' \
#           n8n-secrets.yaml > n8n-secrets.enc.yaml
#      rm n8n-secrets.yaml
apiVersion: v1
kind: Secret
metadata:
  name: n8n-secrets
  namespace: n8n
  labels:
    app.kubernetes.io/name: n8n
    app.kubernetes.io/managed-by: argocd
type: Opaque
stringData:
  encryption-key: "HIER_ENCRYPTION_KEY"
  db-password: "HIER_DASSELBE_DB_PASSWORT"
