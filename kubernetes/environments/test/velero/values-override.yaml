# Velero TEST Override
# Umgebungsspezifisch: S3-Endpoint, Bucket, Backup-Schedule

# -----------------------------------------------------------------------
# Backup Storage Location: TEST S3 Bucket auf NAS10
# -----------------------------------------------------------------------
configuration:
  backupStorageLocation:
    - name: default
      provider: aws
      default: true
      accessMode: ReadWrite
      bucket: k8s-test-velero
      config:
        region: us-east-1
        s3ForcePathStyle: "true"
        s3Url: http://nas10.eneg.de:8010
        insecureSkipTLSVerify: "true"

# -----------------------------------------------------------------------
# Backup Schedule: Taeglich 04:30 Europe/Berlin, 14 Tage Retention
# -----------------------------------------------------------------------
schedules:
  daily-backup:
    disabled: false
    schedule: "30 3 * * *"
    useOwnerReferencesInBackup: false
    template:
      ttl: "336h0m0s"
      storageLocation: default
      defaultVolumesToFsBackup: true
      includedNamespaces:
        - "*"
