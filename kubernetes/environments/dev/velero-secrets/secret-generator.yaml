---
# KSOPS Secret Generator fuer Velero S3 Credentials
# Enthaelt: NAS10 S3 Access/Secret Key im AWS Credentials Format
apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: velero-secret-generator
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: ksops
files:
  - ./velero-s3-credentials.enc.yaml
