# TEMPLATE - Nicht committen! Auf k8s-mgmt-10 ausfuellen und mit SOPS verschluesseln:
# sops --encrypt --encrypted-regex '^(data|stringData)$' \
#   --age age1fdqtcha9jnzqafe5t6hed6v5sv858x2tt6nwuw00u3luyxuaqcxqh5mcrm \
#   vcenter-credentials.yaml > vcenter-credentials.enc.yaml
#
# User: svc-otel-vcenter@eneg.de (AD, Read-Only + Performance.ModifyIntervals)
# Gilt fuer BEIDE vCenter (vcenter-a und vcenter-b, gemeinsame AD-Identity-Source).
# Verifiziert gegen beide vCenter am 03.07.2026 (Login OK, Hosts+Datastores sichtbar).
---
apiVersion: v1
kind: Secret
metadata:
  name: vcenter-credentials
  namespace: monitoring
type: Opaque
stringData:
  VCENTER_USERNAME: "svc-otel-vcenter@eneg.de"
  VCENTER_PASSWORD: "DEIN_VCENTER_PASSWORT"
