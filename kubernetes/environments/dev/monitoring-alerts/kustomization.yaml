apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/monitoring/alert-rules
  - ../../../base/monitoring/servicemonitors
  - ../../../base/monitoring/dashboards
  - ../../../base/monitoring/prometheus-msteams
  - kube-cpu-overcommit-dev.yaml
  - kube-job-failed-dev.yaml
  - prometheus-tsdb-health-dev.yaml
  - vsphere-hosts-dashboard-cm.yaml
  - vsphere-alerts-dev.yaml

# DEV-Patches auf Base-Rules
patches:
  # CronJobOverdue: 36h Threshold, kube_cronjob_status_last_successful_time, idoit im Selector
  # Strategic-Merge-Patch: ersetzt komplette Group "backup.rules" (robust gg. Reihenfolge-Aenderungen)
  - path: backup-alerts-overdue-dev.yaml
