---
# MariaDB Galera Cluster (DEV)
# MariaDB 11.8.6 LTS mit 3 Nodes (Multi-Master)
# Apps: i-doit
apiVersion: k8s.mariadb.com/v1alpha1
kind: MariaDB
metadata:
  name: mariadb-galera
  namespace: databases
  labels:
    app.kubernetes.io/part-of: mariadb-galera
    app.kubernetes.io/managed-by: argocd
spec:
  image: mariadb:11.8.6
  # TEMP 2026-05-14: replicas=0 fuer Cluster-Recovery nach Storage-Storm
  # Siehe docs/incidents/2026-05-11-mariadb-galera-recovery.md
  # Reaktivierung nach Stabilisierung -> replicas=3 (PVCs bleiben durch Operator-Default)
  #
  # replicasAllowEvenNumber=true erlaubt replicas=0 (Operator validiert sonst
  # auf "ungerade Anzahl wegen Split-Brain-Vermeidung"). Bei Reaktivierung mit
  # replicas=3 koennte das Flag wieder raus, schadet aber nicht.
  replicas: 0
  replicasAllowEvenNumber: true
  galera:
    enabled: true
    primary:
      podIndex: 0
      autoFailover: true
    sst: mariabackup
    replicaThreads: 1
    agent:
      gracefulShutdownTimeout: 5s
    recovery:
      enabled: true
      clusterHealthyTimeout: 3m
      clusterBootstrapTimeout: 10m

  rootPasswordSecretKeyRef:
    name: mariadb-credentials
    key: ROOT_PASSWORD
    generate: false

  storage:
    size: 20Gi
    storageClassName: longhorn-db

  resources:
    requests:
      memory: "1Gi"
      cpu: "250m"
    limits:
      memory: "2Gi"
      cpu: "1000m"

  myCnf: |
    [mariadb]
    bind-address=*
    default_storage_engine=InnoDB
    binlog_format=ROW
    innodb_autoinc_lock_mode=2
    innodb_buffer_pool_size=1024M
    innodb_log_file_size=256M
    innodb_flush_log_at_trx_commit=2
    max_allowed_packet=64M
    max_connections=200
    character_set_server=utf8mb4
    collation_server=utf8mb4_unicode_ci
    sql_mode=
    log_warnings=2
    slow_query_log=ON
    long_query_time=2
    default_time_zone=Europe/Berlin

  updateStrategy:
    type: ReplicasFirstPrimaryLast

  metrics:
    enabled: false
