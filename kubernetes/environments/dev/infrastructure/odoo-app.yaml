---
# ArgoCD Application: Odoo 18 CE
# Deployed Namespace, ConfigMap, Deployment, Service, PVC, Ingress
# Resourcen liegen in odoo + traefik Namespace (cross-namespace Ingress)
# Sync-Wave 8: Nach Secrets (Wave 7)
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: odoo
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "8"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: git@github.com:dhenkeeneg/eneg-k8s-infrastructure-v2.git
    targetRevision: main
    path: kubernetes/base/apps/odoo
    directory:
      recurse: false

  destination:
    server: https://kubernetes.default.svc

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
