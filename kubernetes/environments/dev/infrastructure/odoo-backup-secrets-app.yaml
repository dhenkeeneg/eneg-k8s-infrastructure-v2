---
# ArgoCD Application: Odoo Backup Secrets (via KSOPS)
# NAS10 S3-Credentials fuer Filestore-Backup
# Sync-Wave 7: Zusammen mit anderen Secrets
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: odoo-backup-secrets
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "7"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: git@github.com:dhenkeeneg/eneg-k8s-infrastructure-v2.git
    targetRevision: main
    path: kubernetes/environments/dev/apps/odoo/backup/secrets

  destination:
    server: https://kubernetes.default.svc
    namespace: odoo

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
