---
# ArgoCD Application: Velero S3 Secrets (via KSOPS)
# NAS10 S3-Credentials fuer Velero BackupStorageLocation
# Sync-Wave 4: Vor Velero Deployment (Wave 5)
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: velero-secrets
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "4"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: git@github.com:dhenkeeneg/eneg-k8s-infrastructure-v2.git
    targetRevision: main
    path: kubernetes/environments/dev/velero-secrets

  destination:
    server: https://kubernetes.default.svc
    namespace: velero

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
