---
# VORLAGE - NICHT COMMITTEN!
# ghcr.io Image Pull Secret fuer private Container Images (monitoring Namespace)
# Erstellen auf k8s-mgmt-10:
#   1. GitHub PAT mit read:packages Berechtigung (gleicher wie bei idoit/it-info-versand)
#   2. Datei ausfuellen
#   3. Verschluesseln:
#      cd ~/git/eneg-k8s-infrastructure-v2/kubernetes/environments/dev/monitoring-secrets
#      sops --encrypt --age age1fdqtcha9jnzqafe5t6hed6v5sv858x2tt6nwuw00u3luyxuaqcxqh5mcrm \
#           --encrypted-regex '^(data|stringData)$' \
#           ghcr-pull-secret.yaml > ghcr-pull-secret.enc.yaml
#      rm ghcr-pull-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: ghcr-pull-secret
  namespace: monitoring
type: kubernetes.io/dockerconfigjson
stringData:
  .dockerconfigjson: |
    {
      "auths": {
        "ghcr.io": {
          "username": "dhenkeeneg",
          "password": "HIER_GITHUB_PAT_TOKEN_EINTRAGEN",
          "auth": "HIER_BASE64_VON_USERNAME:TOKEN"
        }
      }
    }
