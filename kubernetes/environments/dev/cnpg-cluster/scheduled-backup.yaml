---
# ScheduledBackup: cnpg-shared Full Backup (Barman Cloud Plugin)
# Taeglich um 04:45 UTC (DEV-Backup-Fenster 04:30-06:30 UTC)
# Hinweis: CNPG verwendet 6-Feld-Cron (Sekunde Minute Stunde Tag Monat Wochentag).
# 5-Feld-Cron wird als 6-Feld interpretiert und triggert dann jede Stunde.
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: cnpg-shared-full
  namespace: databases
  labels:
    app.kubernetes.io/part-of: cloudnative-pg
    app.kubernetes.io/managed-by: argocd
spec:
  cluster:
    name: cnpg-shared
  schedule: "0 45 4 * * *"
  backupOwnerReference: cluster
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
  immediate: true
  suspend: false
---
# ScheduledBackup: cnpg-erp Full Backup (Barman Cloud Plugin)
# Taeglich um 04:50 UTC (versetzt zu shared)
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: cnpg-erp-full
  namespace: databases
  labels:
    app.kubernetes.io/part-of: cloudnative-pg
    app.kubernetes.io/managed-by: argocd
spec:
  cluster:
    name: cnpg-erp
  schedule: "0 50 4 * * *"
  backupOwnerReference: cluster
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
  immediate: true
  suspend: false
