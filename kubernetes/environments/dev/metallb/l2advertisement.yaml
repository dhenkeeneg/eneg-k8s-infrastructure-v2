---
# MetalLB L2 Advertisement für DEV-Umgebung
# Gibt die IPs aus dem Pool per ARP im lokalen Netzwerk bekannt
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: dev-l2-advertisement
  namespace: metallb-system
spec:
  ipAddressPools:
    - dev-pool
