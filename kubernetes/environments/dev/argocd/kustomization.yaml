apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Basis: ArgoCD Konfiguration (KSOPS, Cmd-Params)
resources:
  - ../../../base/argocd
  # DEV-spezifischer Ingress (Certificate + IngressRoute)
  - argocd-ingress.yaml

# DEV-spezifische ArgoCD URL in argocd-cm
patches:
  - target:
      kind: ConfigMap
      name: argocd-cm
    patch: |-
      - op: add
        path: /data/url
        value: "https://argocd-dev-v2.eneg.de"
