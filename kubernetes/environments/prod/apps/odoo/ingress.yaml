---
# TLS Zertifikat fuer Odoo (PROD)
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: odoo-tls
  namespace: traefik
spec:
  secretName: odoo-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - odoo.eneg.de
---
# IngressRoute fuer Odoo (PROD)
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: odoo
  namespace: traefik
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`odoo.eneg.de`) && PathPrefix(`/websocket`)
      kind: Rule
      services:
        - name: odoo
          namespace: odoo
          port: 8072
    - match: Host(`odoo.eneg.de`)
      kind: Rule
      services:
        - name: odoo
          namespace: odoo
          port: 8069
  tls:
    secretName: odoo-tls
