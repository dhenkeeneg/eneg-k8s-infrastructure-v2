---
# VORLAGE - NICHT COMMITTEN!
# Diese Datei auf dem Management-Server (k8s-mgmt-10) bearbeiten:
# 1. DB-Passwort: Dasselbe Passwort wie in keycloak-db-credentials (databases Namespace)
# 2. Admin-Passwort generieren: openssl rand -hex 24
# 3. Verschluesseln:
#      cd ~/git/eneg-k8s-infrastructure-v2/kubernetes/base/apps/keycloak/secrets
#      sops --encrypt --age age1fdqtcha9jnzqafe5t6hed6v5sv858x2tt6nwuw00u3luyxuaqcxqh5mcrm \
#           --encrypted-regex '^(data|stringData)$' \
#           keycloak-secrets.yaml > keycloak-secrets.enc.yaml
# 4. Diese Vorlage loeschen, nur .enc.yaml committen
apiVersion: v1
kind: Secret
metadata:
  name: keycloak-secrets
  namespace: keycloak
  labels:
    app.kubernetes.io/name: keycloak
    app.kubernetes.io/managed-by: argocd
type: Opaque
stringData:
  db-password: "HIER_DASSELBE_DB_PASSWORT_WIE_IN_keycloak-db-credentials"
  admin-password: "HIER_GENERIERTES_ADMIN_PASSWORT_EINTRAGEN"
