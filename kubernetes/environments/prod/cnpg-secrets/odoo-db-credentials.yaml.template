---
# VORLAGE - NICHT COMMITTEN!
# PROD-Umgebung: Odoo DB-Credentials
# Auf k8s-mgmt-10:
# 1. Passwort generieren: openssl rand -hex 24 (NUR alphanumerisch, kein base64!)
# 2. Verschluesseln:
#      cd ~/git/eneg-k8s-infrastructure-v2/kubernetes/environments/prod/cnpg-secrets
#      sops --encrypt --age age1fdqtcha9jnzqafe5t6hed6v5sv858x2tt6nwuw00u3luyxuaqcxqh5mcrm \
#           --encrypted-regex '^(data|stringData)$' \
#           odoo-db-credentials.yaml > odoo-db-credentials.enc.yaml
#      rm odoo-db-credentials.yaml
apiVersion: v1
kind: Secret
metadata:
  name: odoo-db-credentials
  namespace: databases
  labels:
    app.kubernetes.io/part-of: cloudnative-pg
    app.kubernetes.io/managed-by: argocd
type: Opaque
stringData:
  username: "odoo"
  password: "HIER_GENERIERTES_PASSWORT_EINTRAGEN"
