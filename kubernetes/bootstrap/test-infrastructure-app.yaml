---
# ArgoCD App-of-Apps: TEST Infrastructure
# Ueberwacht das Verzeichnis environments/test/infrastructure/ und deployed
# automatisch alle ArgoCD Application-Manifests (*-app.yaml) darin.
#
# Einmalig manuell deployen auf dem TEST-Cluster:
#   kubectl apply -f kubernetes/bootstrap/test-infrastructure-app.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: test-infrastructure
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: git@github.com:dhenkeeneg/eneg-k8s-infrastructure-v2.git
    targetRevision: main
    path: kubernetes/environments/test/infrastructure

  destination:
    server: https://kubernetes.default.svc
    namespace: argocd

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
