# Diese Datei wurde nach kubernetes/environments/{dev,test}/longhorn/ verschoben.
# Das Longhorn Dashboard Ingress (Certificate + IngressRoute) wird per
# Environment-Overlay definiert, da der DNS-Name umgebungsspezifisch ist.
# Siehe: kubernetes/environments/{dev,test,prod}/longhorn/ingress.yaml
