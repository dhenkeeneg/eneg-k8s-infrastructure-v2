apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: metallb-system

resources:
  # MetalLB v0.15.3 native (L2) Installation
  - github.com/metallb/metallb/config/native?ref=v0.15.3
  # IP-Pool und L2Advertisement werden per Environment-Overlay definiert
