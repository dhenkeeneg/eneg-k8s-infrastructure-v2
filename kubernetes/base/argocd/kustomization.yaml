apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - argocd-cm.yaml
  - argocd-cmd-params-cm.yaml

# HINWEIS: argocd-repo-server-ksops-patch.yaml wird NICHT hier eingebunden.
# ArgoCD kann sich nicht selbst patchen (Deployment ist nicht Teil dieser Kustomization).
# Der Patch muss manuell per kubectl patch angewendet werden.
# Siehe: docs/phases/phase-04-setup.md und kubernetes/base/argocd/README.md
