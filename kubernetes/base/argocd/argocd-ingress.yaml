# Diese Datei wurde nach kubernetes/environments/{dev,test}/argocd/ verschoben.
# Das ArgoCD Ingress (Certificate + IngressRoute) wird per Environment-Overlay
# definiert, da der DNS-Name umgebungsspezifisch ist.
# Siehe: kubernetes/environments/{dev,test,prod}/argocd/argocd-ingress.yaml
