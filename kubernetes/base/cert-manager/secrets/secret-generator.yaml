---
# KSOPS Secret Generator für IONOS API Credentials
# ArgoCD entschlüsselt dieses Secret automatisch via KSOPS + Age
apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: ionos-secret-generator
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: ksops
files:
  - ./ionos-secret.enc.yaml
