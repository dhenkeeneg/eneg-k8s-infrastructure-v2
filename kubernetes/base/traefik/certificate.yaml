# Diese Datei wurde nach kubernetes/environments/{dev,test}/traefik/ verschoben.
# Das Traefik Dashboard Certificate wird per Environment-Overlay definiert,
# da der DNS-Name umgebungsspezifisch ist.
