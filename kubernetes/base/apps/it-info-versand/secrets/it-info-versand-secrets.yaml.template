---
# VORLAGE - NICHT COMMITTEN!
# Diese Datei auf dem Management-Server (k8s-mgmt-10) bearbeiten:
# 1. Passwoerter/Keys generieren:
#      openssl rand -base64 24          # fuer db-password, session-secret
#      python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"  # encryption-key
# 2. SMTP-Credentials und Keycloak-Secret eintragen
# 3. Verschluesseln:
#      cd ~/git/eneg-k8s-infrastructure-v2/kubernetes/base/apps/it-info-versand/secrets
#      sops --encrypt --age age1fdqtcha9jnzqafe5t6hed6v5sv858x2tt6nwuw00u3luyxuaqcxqh5mcrm \
#           --encrypted-regex '^(data|stringData)$' \
#           it-info-versand-secrets.yaml > it-info-versand-secrets.enc.yaml
# 4. Diese Vorlage loeschen, nur .enc.yaml committen
apiVersion: v1
kind: Secret
metadata:
  name: it-info-versand-secrets
  namespace: it-info-versand
  labels:
    app.kubernetes.io/name: it-info-versand
    app.kubernetes.io/managed-by: argocd
type: Opaque
stringData:
  db-password: "HIER_DASSELBE_PASSWORT_WIE_IN_it-info-versand-db-credentials"
  session-secret: "HIER_GENERIERTEN_SESSION_SECRET_EINTRAGEN"
  encryption-key: "HIER_GENERIERTEN_FERNET_KEY_EINTRAGEN"
  smtp-username: "HIER_SMTP_BENUTZERNAME_EINTRAGEN"
  smtp-password: "HIER_SMTP_PASSWORT_EINTRAGEN"
  keycloak-client-secret: "PLATZHALTER_PHASE_2"
